# Copia a .env (no subir a git). En producción usa variables en Cloud Run / Firebase.

# Correo con QR (Resend, opcional): https://resend.com
# Si no lo configuras, el reenvío de QR por correo queda deshabilitado (todo lo demás funciona).
RESEND_API_KEY=re_xxxxx
RESEND_FROM=Festi <onboarding@resend.dev>
